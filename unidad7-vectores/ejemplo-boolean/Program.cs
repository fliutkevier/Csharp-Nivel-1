using System;

namespace ejemplo_boolean
{
    class Program
    {
        static void Main(string[] args)
        {
            //El bool o boolean, permite guardar true o false 1 o 0.
            //bool algo = true; (comentado porque da error rompe huevos)

            bool[] asistenciasQuincena = new bool[15];
            for (int i = 0; i < 15; i++)
            {
                //va a cargar si en el día asistió o no.

            }

            //otro ejemplo de clase
            int[] legajos = new int[100];
            bool[] asistencia = new bool[100];
            
            for (int i = 0; i < 16; i++)
            {
                for (int x = 0; x < 100; x++)
                {
                    
                }
            }
        }
    }
}
